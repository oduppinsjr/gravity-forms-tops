<?php
/**
 * Reserved for shared plugin constants.
 *
 * TowX JSON (GetMakes, etc.) and Create Call XML both use the same base URL, chosen under
 * Forms → Settings → TOPS → API environment (production / QA / custom).
 *
 * @package GF_Tops
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
